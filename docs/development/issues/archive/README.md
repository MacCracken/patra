# Archived Issues

Resolved upstream-tracking issues — cyrius bugs and cross-target ABI gaps — that surfaced during patra development. Each archived file keeps its original body verbatim plus an `ARCHIVED YYYY-MM-DD — RESOLVED …` block at the top. Don't rewrite history here — the archive's value is being a faithful record of what was filed and what fixed it.

Open issues sit at the parent [`docs/development/issues/`](../) directory. Issues move here when the upstream fix lands and the workaround is no longer load-bearing.

## Index

| File | Filed | Resolved | Hook |
|---|---|---|---|
| [`2026-04-30-cyrius-cyrfmt-cyrlint-buffer-truncation.md`](2026-04-30-cyrius-cyrfmt-cyrlint-buffer-truncation.md) | 2026-04-30 (cyrius 5.7.48) | 2026-05-21 (cyrius 6.0.1) | Internal 128 KB read buffer in `cyrfmt` / `cyrlint` silently truncated `.cyr` / `.tcyr` files past the cap. Cyrius 6.0.1 raised the buffer 128 KB → 512 KB (4×); patra's `tests/tcyr/patra.tcyr` is 130,692 bytes after v1.9.2's ASCII pass, well under the new cap. Same fixed-buffer *shape* still exists at 4× the size — re-file if a future test set crosses 512 KB. |
| [`2026-05-27-cyrius-distlib-blank-lines.md`](2026-05-27-cyrius-distlib-blank-lines.md) | 2026-05-27 (cyrius 6.0.3) | 2026-06-25 (confirmed cyrius 6.2.44) | `cyrius distlib` emitted 3 cyrlint "multiple consecutive blank lines" warnings in `dist/patra.cyr` (4-line-header separator + `include`-strip residue) though every `src/*.cyr` module lints clean. distlib now collapses the blank runs: regenerating under 6.2.44 + `cyrius lint dist/patra.cyr` reports 0 warnings. The deliberately-skipped source workaround was never needed. Confirmed during the patra 1.12.5 pin bump. |
| [`2026-06-09-cyrius-no-portable-mutex.md`](2026-06-09-cyrius-no-portable-mutex.md) | 2026-06-09 (cyrius 6.1.15) | 2026-06-17 (cyrius 6.2.x) | No portable stdlib mutex — the only lock lived inside `lib/thread.cyr`'s Linux clone/futex machinery (can't parse under Win32), so patra hand-rolled an inline futex mutex (`_patra_mtx`) for v1.11.0 thread-safety. Cyrius now ships `lib/sync.cyr` (+ `sync_macos`/`sync_windows`) — the lock alone, per-OS selected, with a stated memory-ordering contract. Patra migrated onto it in v1.11.4 (hand-rolled inline futex removed). |
| [`2026-06-18-agnos-cross-target-abi.md`](2026-06-18-agnos-cross-target-abi.md) | 2026-06-18 (agnos 1.46 / patra 1.11.4-vendored) | 2026-06-25 (patra 1.12.5) | patra's seek-based page engine was "incompatible" with agnos — no `lseek`/`pread`/`flock` — and the issue demanded an architecture call (mmap backend vs. kernel ask vs. defer). Overtaken by events: agnos 1.46 added `lseek` #58 + `flock` #59 via the syscall peer, so the existing engine works behind per-target `#ifdef` guards (adopted 1.12.2/1.12.3); the last `sys_unlink` ABI wart was routed through `io.cyr` `xunlink` in 1.12.5. `cyrius build --agnos src/lib.cyr` now cross-compiles warning-free. No mmap backend needed. |
| [`2026-08-30-pcache-publish-before-fill.md`](2026-08-30-pcache-publish-before-fill.md) | 2026-08-30 (patra 1.13.10) | 2026-08-30 (patra 1.13.11) | `_pc_alloc` assigned `_pc_keys` — the global its own guard tests — before allocating `_pc_bufs` or filling either table, and ran outside `_pc_mtx`. Two concurrent `patra_cache_enable(1)` calls could therefore arm the cache (`_pc_on = 1`) over an unbuilt pool, making `_pc_put`'s `load64(_pc_bufs + slot * 8)` a read through a null base. Fixed by allocating under `_pc_mtx` and publishing `_pc_keys` last. ⚠ **Never reproduced** — ~1,600 runs across four harness shapes found zero occurrences pre-fix (detector validated against hand-built bad states), and reaching it needs a concurrent `patra_cache_enable`, which the API already forbids. Real by inspection, free to fix, not demonstrated. Found by a samay concurrency audit; the `chrono` sibling is cyrius's and filed upstream. |
