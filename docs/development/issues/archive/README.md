# Archived Issues

Resolved upstream-tracking issues — cyrius bugs and cross-target ABI gaps — that surfaced during patra development. Each archived file keeps its original body verbatim plus an `ARCHIVED YYYY-MM-DD — RESOLVED …` block at the top. Don't rewrite history here — the archive's value is being a faithful record of what was filed and what fixed it.

Open issues sit at the parent [`docs/development/issues/`](../) directory. Issues move here when the upstream fix lands and the workaround is no longer load-bearing.

## Index

| File | Filed | Resolved | Hook |
|---|---|---|---|
| [`2026-04-30-cyrius-cyrfmt-cyrlint-buffer-truncation.md`](2026-04-30-cyrius-cyrfmt-cyrlint-buffer-truncation.md) | 2026-04-30 (cyrius 5.7.48) | 2026-05-21 (cyrius 6.0.1) | Internal 128 KB read buffer in `cyrfmt` / `cyrlint` silently truncated `.cyr` / `.tcyr` files past the cap. Cyrius 6.0.1 raised the buffer 128 KB → 512 KB (4×); patra's `tests/tcyr/patra.tcyr` is 130,692 bytes after v1.9.2's ASCII pass, well under the new cap. Same fixed-buffer *shape* still exists at 4× the size — re-file if a future test set crosses 512 KB. |
| [`2026-05-27-cyrius-distlib-blank-lines.md`](2026-05-27-cyrius-distlib-blank-lines.md) | 2026-05-27 (cyrius 6.0.3) | 2026-06-25 (confirmed cyrius 6.2.44) | `cyrius distlib` emitted 3 cyrlint "multiple consecutive blank lines" warnings in `dist/patra.cyr` (4-line-header separator + `include`-strip residue) though every `src/*.cyr` module lints clean. distlib now collapses the blank runs: regenerating under 6.2.44 + `cyrius lint dist/patra.cyr` reports 0 warnings. The deliberately-skipped source workaround was never needed. Confirmed during the patra 1.12.5 pin bump. |
| [`2026-06-09-cyrius-no-portable-mutex.md`](2026-06-09-cyrius-no-portable-mutex.md) | 2026-06-09 (cyrius 6.1.15) | 2026-06-17 (cyrius 6.2.x) | No portable stdlib mutex — the only lock lived inside `lib/thread.cyr`'s Linux clone/futex machinery (can't parse under Win32), so patra hand-rolled an inline futex mutex (`_patra_mtx`) for v1.11.0 thread-safety. Cyrius now ships `lib/sync.cyr` (+ `sync_macos`/`sync_windows`) — the lock alone, per-OS selected, with a stated memory-ordering contract. Patra migrated onto it in v1.11.4 (hand-rolled inline futex removed). |
| [`2026-06-18-agnos-cross-target-abi.md`](2026-06-18-agnos-cross-target-abi.md) | 2026-06-18 (agnos 1.46 / patra 1.11.4-vendored) | 2026-06-25 (patra 1.12.5) | patra's seek-based page engine was "incompatible" with agnos — no `lseek`/`pread`/`flock` — and the issue demanded an architecture call (mmap backend vs. kernel ask vs. defer). Overtaken by events: agnos 1.46 added `lseek` #58 + `flock` #59 via the syscall peer, so the existing engine works behind per-target `#ifdef` guards (adopted 1.12.2/1.12.3); the last `sys_unlink` ABI wart was routed through `io.cyr` `xunlink` in 1.12.5. `cyrius build --agnos src/lib.cyr` now cross-compiles warning-free. No mmap backend needed. |
| [`2026-06-28-concurrent-read-table-lookup-cache-race.md`](2026-06-28-concurrent-read-table-lookup-cache-race.md) | 2026-06-28 (patra 1.12.6) | 2026-06-29 (patra 1.12.7) | The tail-page lookup cache lived in process-global `_tbl_lp_idx` / `_tbl_lp_page`, so inserts through a *different handle* — acutely, a different file sharing a table index — could trust a stale cached page. Moved into the db handle (`DB_LP_IDX` / `DB_LP_PAGE` / `DB_LP_GEN`) and gen-gated on the on-disk `HDR_COMMITGEN`, with the gen carried forward across a handle's own commit so there is no perf cost. ⚠ The original **"reader-vs-reader" framing was imprecise** — the cache is written only on the serialized insert path and never read by `SELECT`; the real defect was cross-*handle* inserts. Regression `tail-page cache per-handle`, verified to fail against a simulated process-global cache. |
| [`2026-07-03-text-blob-readback-escapes-query-flock-window.md`](2026-07-03-text-blob-readback-escapes-query-flock-window.md) | 2026-07-03 (patra 1.12.7, filed by `yeo-cy-test`) | 2026-07-03 (patra 1.12.8) | **HIGH — silent data corruption.** `TEXT`/`BYTES` result cells were read lazily by walking the chain *after* the query released its flock, so a concurrent writer could free and reuse those pages under an open result set — defeating the headline promise of the connection-per-thread model. `_rs_materialize` now snapshots every chain payload into an owned heap buffer while the shared flock is still held, making `patra_result_read_text` / `_read_bytes` pure memcpys freed by `patra_result_free`. No API change, no lock held across iteration. Regression `text readback snapshot (flock-window fix)`, verified to fail pre-fix. |
| [`2026-08-18-cyrius-distlib-named-deps-unanchored-scan.md`](2026-08-18-cyrius-distlib-named-deps-unanchored-scan.md) | 2026-08-18 (cyrius 6.5.27) | 2026-08-18 (cyrius 6.5.28) | `cyrius distlib`'s `_distlib_named_deps` scanned for the literal `[deps.` with no line anchoring, so a bracketed deps header written in **comment prose** registered as a named dep and deleted a real leaf from the sidecar — patra shipped **11 leaves against 12**, missing `sakshi`, from ≤1.12.11 through 1.13.1. Fixed upstream **the same day it was filed** (the parser fix landed 14 minutes after the upstream filing): the scan is now anchored to a line start and comment-aware. ⚠ **Stale for three shipped cuts** — patra's pin reached 6.5.29 at v1.13.9, and 1.13.9 / 1.13.10 / 1.13.11 each bumped the toolchain without re-triaging it; archived at v1.13.12. Mutation-verified under 6.6.0 rather than taken on the CHANGELOG's word (three comment shapes still emit 12 leaves; a real `[deps.sakshi]` header emits 11, proving the probe discriminates). Backtick convention + the v1.13.7 CI leaf-count gate both retained as defence in depth. Two narrow residual holes upstream (unbounded `]` search; `"""` strings) are recorded in the archived file. |
| [`2026-09-07-schema-load-prologue-cloned-ten-times.md`](2026-09-07-schema-load-prologue-cloned-ten-times.md) | 2026-09-07 (patra 1.14.0) | 2026-09-07 (patra 1.14.1) | Ten statement paths carried the same six-line table-open prologue and **all ten discarded `page_read`'s return** — and `pg_alloc` recycles slab pages *without zeroing*, so a failed read left the previous occupant's bytes, most plausibly another table's schema, which the statement then used while reporting `PATRA_OK`. v1.14.0 mitigated it by zeroing (a silent no-op); v1.14.1 extracted `_tbl_open`, which returns a status all ten callers propagate. ⚠ The "ten different cleanup paths" the filing deferred over turned out not to apply *at the prologue* — the schema page is the only allocation at that point and the helper owns it, so every error path is uniformly `_tx_unlock; return tor;`. Leak asserted via `TLS_SLAB_TOP` accounting over 64 failed and 64 successful opens; mutation-verified three ways. Also fixed a second defect it exposed: the query path was the only site taking its column count from `TBL_NCOLS` rather than the schema page. |
| [`2026-08-30-pcache-publish-before-fill.md`](2026-08-30-pcache-publish-before-fill.md) | 2026-08-30 (patra 1.13.10) | 2026-08-30 (patra 1.13.11) | `_pc_alloc` assigned `_pc_keys` — the global its own guard tests — before allocating `_pc_bufs` or filling either table, and ran outside `_pc_mtx`. Two concurrent `patra_cache_enable(1)` calls could therefore arm the cache (`_pc_on = 1`) over an unbuilt pool, making `_pc_put`'s `load64(_pc_bufs + slot * 8)` a read through a null base. Fixed by allocating under `_pc_mtx` and publishing `_pc_keys` last. ⚠ **Never reproduced** — ~1,600 runs across four harness shapes found zero occurrences pre-fix (detector validated against hand-built bad states), and reaching it needs a concurrent `patra_cache_enable`, which the API already forbids. Real by inspection, free to fix, not demonstrated. Found by a samay concurrency audit; the `chrono` sibling is cyrius's and filed upstream. |
| [`2026-09-21-raw-syscall-sweep-and-gate.md`](2026-09-21-raw-syscall-sweep-and-gate.md) | 2026-09-21 (patra 1.14.3, from libro 2.10.3) | 2026-09-23 (patra 1.15.0) | **345 lines of raw `syscall(…)`** (346 calls) — 24 in `src/`, 321 lines in the harnesses — each hard-coding one target's ABI where a stdlib wrapper existed. All replaced (`xlseek`, `xflock`, `random_bytes`, `clock_epoch_secs`, `sys_exit`, `xunlink`, `file_open`, …); fdatasync through one `_pt_fdatasync` (`sys_fdatasync` on Linux/macOS, `xfsync` elsewhere); `file.cyr`'s private `SYS_*` / `LOCK_*` tables deleted; `[deps] stdlib` 12 → 14 leaves; a CI gate that also rejects numeric open flags, mutation-verified (345 + 14 hits on the pre-sweep tree). ⚠ **It found three defects the filing did not list**, all by inspection of the peers: macOS never used the CSPRNG (getentropy's 0-on-success read as failure), macOS could not create a database or truncate a WAL (`194` / `578` open flags), and Windows failed every write inside a transaction (unrouted fdatasync → -ENOSYS, checked by `wal_log_page`). The harnesses now build and pass on aarch64 under qemu (1301/1301, 8/8, 3/3); before, most did not compile there. |
